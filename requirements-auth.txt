# 로그인/회원가입 기능 — 강한 암호 구현(권장).
#   bcrypt        : 비밀번호 해싱 (보안 기획안 채택안)
#   cryptography  : PII(이름·관심조건) Fernet 암호화
#
# 이 둘이 없어도 app.py 는 실행된다. 미설치 시 rag_chatbot.auth.crypto 가
# 표준 라이브러리 대체 구현(PBKDF2 / HMAC 기반 인증 암호화)으로 자동 전환한다.
# 운영 배포에서는 아래를 설치해 강한 구현을 쓰는 것을 권장한다.
bcrypt>=4.0
cryptography>=42.0

# 원격 회원 DB(RunPod Pod 등의 MySQL/MariaDB)를 쓸 때만 필요 — 기본은 SQLite 라
# 평소엔 설치하지 않아도 된다. AUTH_DB_URL 을 설정하는 사람만 아래 주석을 풀거나
#   pip install pymysql
# 을 실행한다. 배포 절차: docs/AUTH_REMOTE_DB.md
# pymysql>=1.1
