# N5 토큰 상향 후 LLM 실패 문항 재실행 리포트 (2026-09-20)

a(2026-09-19 오전, `test-20260919`) 평가에서 LLM 호출이 실패했던 **23문항만** N5 토큰을 올린 코드로 다시 돌려 a 결과에 반영했습니다. 나머지 327문항은 a 결과 그대로입니다.
350문항 합산은 개발(Dev) 평가이며 Gate 6 Holdout이 아닙니다. `quasi_holdout`은 "추가 Dev" 세트입니다.

## 1. 결론

**N5 토큰 상향으로 LLM 호출 실패가 27건에서 2건으로 줄었고(0.92% → 0.07%), 품질 지표는 변동 범위 안에서 그대로입니다.** 개선은 "실패 제거"이지 답변 품질 향상이 아닙니다.

| 지표 (350문항, 재실행 반영) | a | 반영 후 | 차이 |
| --- | ---: | ---: | ---: |
| LLM 호출 실패율 | 0.92% (27/2935) | **0.07% (2/2916)** | -0.85%p |
| Recall@5 | 0.792 | 0.792 | 0 |
| MRR@5 | 0.680 | 0.677 | -0.003 |
| Citation Precision | 0.319 (255/799) | 0.324 (255/787) | +0.005 |
| Abstention Precision | 0.850 (17/20) | 0.857 (18/21) | +0.007 |
| Abstention Recall | 0.607 (17/28) | 0.643 (18/28) | +0.036 |
| Faithfulness | 0.711 (277건) | 0.716 (276건) | +0.005 |
| Answer Relevancy | 0.655 (306건) | 0.645 (303건) | -0.010 |

## 2. 변경 내용과 방법

**코드 변경 (N5만, N13은 그대로)**
- `claim_extractor.py`: N5의 LLM 호출에 `max_tokens=2048`을 지정합니다. 환경변수 `LLM_MAX_NEW_TOKENS_CLAIM_EXTRACT`로 바꿀 수 있고, 호출 시점에 읽으며 잘못된 값이면 기본값 2048로 돌아갑니다.
- **프롬프트는 바꾸지 않았습니다.** 이전에 시도한 출력 형식 힌트는 롤백했습니다(`fe657e9`).
- 견고성 수정 2건을 함께 넣었습니다. 토큰이 늘면 잘렸던 응답이 끝까지 나와 파싱에 성공하는 경우가 늘 수 있고, 그때 N7이 `ValueError`를 던질 수 있는 경로를 미리 막습니다: 지원하지 않는 `required_aspects` 제거, 중복 `reasons` 제거.
- `llm_prompt_probe.py`의 래퍼가 `max_tokens`를 받도록 고쳤습니다.
- 테스트를 추가했고 관련 36건이 통과했습니다.

**재실행 방법**
- 대상: a 실행에서 `llm_status.failures > 0`이었던 문항 23개 (dev150 9, policy100 2, 추가 Dev 12).
- 재실행은 파이프라인과 답변 품질 판정(LLM-judge)까지 전부 수행했고, 3개 세트 모두 `quality_metrics_valid=True`, 문항 오류 0입니다.
- 병합: a의 `results_*.jsonl`에서 이 23문항만 재실행 결과로 바꾼 뒤, 프로젝트의 기존 계산 함수(`calculate_summary`, `summarize_answer_quality`)로 지표를 다시 계산했습니다(`scripts/merge_evaluation_reruns.py`). 점수 계산식은 바꾸지 않았습니다.

## 3. 세트별 결과

| 세트 | 지표 | a | 반영 후 |
| --- | --- | ---: | ---: |
| dev150 | LLM 실패 / 호출 | 11 / 927 | 1 / 920 |
|  | Recall / MRR | 0.810 / 0.714 | 0.810 / 0.718 |
|  | Citation Precision | 0.397 | 0.405 |
|  | 예측 보류 / 정답 보류 | 7 / 8 | 8 / 8 |
|  | Faithfulness / Relevancy | 0.733 / 0.632 | 0.733 / 0.633 |
| policy100 | LLM 실패 / 호출 | 2 / 1054 | 0 / 1050 |
|  | Recall / MRR | 0.790 / 0.655 | 0.790 / 0.655 |
|  | Citation Precision | 0.288 | 0.287 |
|  | Faithfulness / Relevancy | 0.719 / 0.790 | 0.719 / 0.776 |
| 추가 Dev | LLM 실패 / 호출 | 14 / 954 | 1 / 946 |
|  | Recall / MRR | 0.762 / 0.652 | 0.762 / 0.631 |
|  | Citation Precision | 0.260 | 0.268 |
|  | 예측 보류 / 정답 보류 | 13 / 20 | 13 / 20 |
|  | Faithfulness / Relevancy | 0.667 / 0.556 | 0.683 / 0.534 |

## 4. 재실행한 23문항의 변화

- **LLM 실패:** 23문항 중 21문항에서 사라졌습니다. 남은 2문항(`dev-form-marine-145`, `pol-391000000152`)은 여전히 `max_new_tokens=2048` 안에 답을 못 쓴 길이 초과입니다. 앞서 짧은 프롬프트인데도 2048을 넘겼던 경우와 비슷해, 한도 부족이 아니라 끝없이 생성하는 경우로 보입니다(원인은 확인하지 않음).
- **정답 정책 검색(hit) 변화:** 2건이 바뀌었습니다(`pol-404000000105` 실패 → 성공, `pol-442000000107` 성공 → 실패). 서로 상쇄돼 Recall은 그대로입니다.
- **보류 변화:** `dev-no-evidence-legal-interpretation-024`가 보류가 아니던 것에서 보류로 바뀌었습니다. 이 문항은 정답이 "보류"이므로 Abstention의 정답 예측이 1건 늘어난 것입니다(합산 Abstention Recall 0.607 → 0.643의 원인).
- **판정 점수:** 문항마다 오르내림이 섞여 있고 방향성이 없습니다. 판정 가능 건수도 문항별로 바뀌었습니다(재실행에서 판정이 `None`이 된 것도, 새로 판정된 것도 있음).

## 5. 해석과 한계

- **재실행은 답변까지 다시 생성한 것입니다.** 실패한 LLM 호출 하나만 다시 한 것이 아니라 그 문항의 파이프라인 전체와 판정을 다시 돌렸으므로, LLM의 비결정성 때문에 실패와 무관한 점수도 바뀝니다. 위 변화 중 어디까지가 토큰 상향 덕이고 어디부터가 변동인지는 구분할 수 없습니다.
- **품질 지표 차이(±0.01 안팎)는 변동 범위 안입니다.** 이 리포트로 말할 수 있는 것은 "실패가 사라졌다"뿐이고, "품질이 좋아졌다"는 아닙니다.
- **코드 상태가 문항마다 다릅니다.** 재실행 23문항은 커밋 `5f5a98f`(검색어 정제, 평가 근거 구성 변경) 위에서 돌았지만, 나머지 327문항은 그 이전 코드로 돌았던 a 결과입니다. 그래서 반영 후 수치는 서로 다른 시점의 코드에서 나온 결과를 합친 것입니다. 이 차이가 지표에 준 영향은 분리하지 못했습니다.
- **부분 재실행 결과입니다.** 전체 350문항을 새 코드로 다시 돌린 결과가 아닙니다. 전체 재실행에서는 잘리지 않아도 새로 실패하는 문항이 나올 수 있어, 위 0.07%는 이 병합 방식에서의 값입니다.
- N5가 규칙 기반으로 폴백되는 비율은 이번에 측정하지 않았습니다. 이전 진단에서 기존 프롬프트는 N5의 LLM 출력 대부분(53/54)이 형식 이탈로 폴백됐습니다. 토큰 상향은 잘림 실패만 없앨 뿐 이 폴백은 그대로일 가능성이 큽니다.
- N13은 이번에 올리지 않았습니다. a 로그에서 N13의 길이 초과는 1건뿐이었고 N13 실패의 대부분은 토큰과 무관한 JSON 파싱 오류였습니다.
- Faithfulness·Answer Relevancy는 판정 LLM이 답변 생성 모델과 같아 자기평가 편향이 있고, 판정 표본은 350문항 중 276~303건입니다.

## 6. 재현

```powershell
$env:PYTHONUTF8="1"
python scripts/merge_evaluation_reruns.py --base <a results jsonl> --rerun <재실행 results jsonl> --name dev150
# 재실행 산출물: artifacts/evaluation/20260920e/{dev150,policy100,holdout100}
# 병합 요약: artifacts/evaluation/20260920e/merged_summary.json
```
