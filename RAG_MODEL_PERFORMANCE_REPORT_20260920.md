# 모델 성능 평가 리포트 (현재 버전, 2026-09-20)

현재 버전의 350문항 평가 결과 표(`RAG_FINAL_REPORT_20260920.md`)만을 근거로 시스템 성능을 평가합니다. 다른 버전과의 비교는 하지 않습니다.

- **평가 대상:** Qwen/Qwen3.5-9B를 쓰는 RAG 파이프라인 전체(검색 → 자격 판정 → 답변 생성). LLM 단독 성능이 아니라 시스템 성능입니다.
- **평가 세트:** dev150 + policy100 + 추가 Dev 100 = 350문항(정답 정책이 있는 322건, 정답이 "보류"인 28건). 개발(Dev) 평가이며 Gate 6 Holdout이 아닙니다.
- **점수 체계:** 검색·인용·보류 지표는 0~1, Faithfulness·Answer Relevancy는 LLM 판정 점수의 평균(0~1)입니다.

## 1. 결과 표

**350문항 합산**

| 영역 | 지표 | 값 | 근거 |
| --- | --- | ---: | --- |
| 검색 | Recall@5 | 0.792 | 정답 문항 322건 |
| 검색 | MRR@5 | 0.677 | 〃 |
| 인용 | Citation Precision | 0.324 | 255 / 787 |
| 인용 | Citation Coverage | 0.792 | Recall과 수치상 동일 |
| 보류 | Abstention Precision | 0.857 | 18 / 21 |
| 보류 | Abstention Recall | 0.643 | 18 / 28 |
| 답변 품질 | Faithfulness | 0.716 | 판정 276건 |
| 답변 품질 | Answer Relevancy | 0.645 | 판정 303건 |
| 안정성 | LLM 호출 실패율 | 0.07% | 2 / 2916 |
| 안정성 | 문항 실행 오류 | 0건 | 350문항 전부 완료 |

**세트별**

| 지표 | dev150 | policy100 | 추가 Dev 100 |
| --- | ---: | ---: | ---: |
| Recall / MRR | 0.810 / 0.718 | 0.790 / 0.655 | 0.762 / 0.631 |
| Citation Precision | 0.405 | 0.287 | 0.268 |
| Abstention Precision / Recall | 0.750 / 0.750 | 해당 없음 | 0.923 / 0.600 |
| Faithfulness | 0.733 | 0.719 | 0.683 |
| Answer Relevancy | 0.633 | 0.776 | 0.534 |
| LLM 호출 실패 / 호출 | 1 / 920 | 0 / 1050 | 1 / 946 |
| 응답시간 p50 / p95 | 50.0s / 160.2s | 63.3s / 141.2s | 57.7s / 187.3s |

## 2. 영역별 평가

### 2-1. 검색 (Recall 0.792, MRR 0.677)
- 정답 정책이 상위 5개 안에 들어오는 문항이 약 10건 중 8건입니다. 나머지 약 2건은 정답 정책이 5개 안에 없어서 그 뒤 단계에서 정답을 만들 수 없습니다. 이 시스템의 **가장 큰 상한 요인은 검색 재현율**입니다.
- MRR이 Recall보다 0.115 낮다는 것은 정답이 들어와도 1순위가 아닌 경우가 상당하다는 뜻입니다(정답 순위의 역수 평균이 0.677). 순위 조정(재랭킹)의 여지가 있다는 신호이나, 이 프로젝트는 재랭킹을 도입하지 않은 구조입니다.
- 세트별로 dev150(0.810) > policy100(0.790) > 추가 Dev(0.762) 순이며, 개발에 쓰지 않은 정책에서 뽑은 추가 Dev에서 가장 낮습니다. 그 외 두 세트보다 처음 보는 정책에서 검색이 약한 경향이 보이지만, 세트 간 차이(0.05 안팎)가 표본 변동보다 큰지는 이 표만으로 판단할 수 없습니다.

### 2-2. 인용 (Citation Precision 0.324)
- 답변이 인용한 정책 중 정답 정책인 비율이 약 3건 중 1건입니다. **수치상 가장 낮은 지표**입니다.
- 이 값은 구조적 상한이 있습니다. 답변이 한 번에 최대 5개 정책을 인용하는데 정답은 대개 1개라서, 정답을 정확히 인용해도 Precision은 1보다 훨씬 낮게 나옵니다. 그래서 이 수치는 "인용이 틀린 비율"이 아니라 "여러 정책을 함께 보여주는 답변 방식과 정책 ID 단위 정의의 결과"에 가깝습니다.
- Coverage(0.792)는 Recall과 같은 값이라, 정답 정책이 검색되면 인용에도 들어가는 것으로 볼 수 있습니다.
- 세트별로 dev150(0.405)이 다른 두 세트(0.287, 0.268)보다 높습니다.

### 2-3. 보류(Abstention) 판단 (Precision 0.857, Recall 0.643)
- **보류할 때는 대체로 맞습니다.** 보류로 판단한 21건 중 18건이 실제 정답 보류입니다(오보류 3건).
- **보류해야 할 질문의 약 3분의 1(28건 중 10건)은 놓칩니다.** 근거가 없는 질문에도 답을 내놓는 경우입니다. 근거가 없는데 답을 지어내는 것은 복지 안내에서 위험한 유형이라 재현율이 상대적으로 더 중요한 약점입니다.
- 세트별로 dev150은 8건 중 6건(0.750/0.750), 추가 Dev는 20건 중 12건(재현율 0.600, 정밀도 0.923)을 잡습니다. policy100에는 정답 보류가 없고 예측 보류도 0건이라, 이 세트에서 불필요한 보류(오보류)는 없었습니다.
- **주의:** 정답 보류가 28건뿐이라 표본이 작습니다. 이 영역의 수치는 변동이 큽니다.

### 2-4. 답변 품질 (Faithfulness 0.716, Answer Relevancy 0.645)
- Faithfulness 0.716: 답변이 근거 문서와 일치하는 정도의 평균입니다. 판정 점수 체계로 보면 "대체로 근거에 기반하지만 일부 부분근거·불일치가 섞인" 수준입니다.
- Answer Relevancy 0.645: 답변이 질문에 맞는 정도의 평균으로, 두 품질 지표 중 더 낮습니다. 질문과 어긋나거나 일부만 답하는 답변이 적지 않다는 뜻입니다.
- 세트 간 편차가 큽니다. Answer Relevancy는 policy100이 0.776인 반면 추가 Dev는 0.534입니다. Faithfulness는 dev150(0.733)에서 추가 Dev(0.683)까지 0.05 차이입니다. **처음 보는 정책(추가 Dev)에서 두 품질 지표가 모두 가장 낮습니다.** 검색 지표(2-1)와 같은 방향입니다.
- 두 지표 모두 판정 LLM이 답변 생성 모델과 같아 자기평가 편향이 있고, 판정 표본이 350문항 중 276~303건입니다. 절대 수준보다 상대 비교의 참고로 쓰는 것이 적절합니다.

### 2-5. 안정성과 응답 시간
- **매우 안정적입니다.** 350문항이 모두 오류 없이 완료됐고, LLM 호출 실패는 2,916건 중 2건(0.07%)입니다.
- **응답이 느립니다.** p50이 50~63초, p95가 141~187초입니다. 한 질문이 여러 번의 LLM 호출(2,916건/350문항 ≈ 문항당 약 8회)과 되묻기 대화를 거치기 때문입니다. 챗봇 서비스로 쓰기에는 사용자 대기 시간이 긴 편입니다. 이 값은 평가 조건(병렬 실행, 무료 provider)에서 측정한 것이라 실제 서비스 지연과는 다를 수 있습니다.

## 3. 종합 평가

| 영역 | 판단 | 근거 |
| --- | --- | --- |
| 안정성 | 우수 | 오류 0건, LLM 실패 0.07% |
| 보류 시 정확도 | 양호 | Precision 0.857 |
| 검색 | 보통 | Recall 0.792, 정답 약 2/10 누락 |
| 답변 근거 충실성 | 보통 | Faithfulness 0.716 |
| 보류 재현율 | 미흡 | Recall 0.643, 근거 없는 질문 약 1/3에 답변 |
| 답변 관련성 | 미흡 | Answer Relevancy 0.645, 세트 간 편차 큼 |
| 인용 정확도 | 낮음(지표 구조 영향) | Citation Precision 0.324 |
| 응답 시간 | 느림 | p50 50~63초 |

**한 줄 요약:** 시스템은 안정적으로 끝까지 동작하고 근거가 없다고 판단할 때는 대체로 정확하지만, 정답 정책을 놓치거나(약 20%) 근거가 없는 질문에 답하는 경우(약 36%)와 질문에 덜 맞는 답변이 남아 있고, 처음 보는 정책에서 성능이 더 낮으며 응답이 느립니다.

## 4. 개선 우선순위 (표에서 읽히는 것)

1. **검색 재현율·순위:** 상한을 결정하는 요인입니다(Recall 0.792, MRR 0.677). 특히 추가 Dev처럼 새 정책에서의 성능 저하가 큽니다.
2. **보류 재현율:** 근거 없는 질문에 답하는 것을 줄여야 합니다(정답 보류의 약 36%를 놓침). 표본을 늘려 재측정이 필요합니다.
3. **답변 관련성:** 두 품질 지표 중 더 낮고 세트 간 편차가 큽니다.
4. **응답 시간:** 서비스 관점에서 대기 시간이 깁니다.
5. **인용 지표 정의:** Citation Precision의 구조적 상한 때문에 정답 인용 여부를 더 정확히 보는 지표(문항당 정답 정책 인용 여부 등)로 보완할지 결정이 필요합니다.

## 5. 이 평가의 한계

- 개발 세트 평가이며 독립 Holdout이 아닙니다. 위 수치는 낙관적일 수 있습니다.
- 표본이 작은 영역이 있습니다(정답 보류 28건).
- 이 표는 일부 문항(LLM 호출이 실패했던 23문항)을 다시 실행해 반영한 결과입니다. 그 문항들의 수치는 재실행 시점의 코드에서 나온 것이며, 실행 간 변동은 측정하지 않았습니다.
- LLM 판정 지표는 자기평가 편향이 있습니다.
- 응답 시간 값은 서로 다른 시점 실행의 지연을 합친 값입니다.
