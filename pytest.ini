# 최소 설정. 목적은 두 가지뿐이다.
#
# 1. rootdir을 이 레포로 고정한다. 설정 파일이 하나도 없으면 pytest가
#    "인자들의 공통 조상"을 rootdir으로 잡는데, tests/ 안에서 실행하면
#    rootdir이 tests/가 되어 레포 루트의 conftest.py를 못 읽는다.
#    그러면 import 경로 설정이 통째로 날아간다.
# 2. 인자 없이 `pytest`만 쳐도 tests/를 돌게 한다.
[pytest]
testpaths = tests
