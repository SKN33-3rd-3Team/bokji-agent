# RAG 최종 평가 리포트 (2026-09-20)

3종 질문 세트(dev150 + policy100 + 추가 Dev 100 = 350문항)에 대한 최종 결과입니다. 2026-09-19 전체 평가(a, `test-20260919`)를 기준으로, 그 실행에서 LLM 호출이 실패한 23문항만 N5 토큰을 올린 코드로 다시 돌려 반영했습니다.
합산 350문항은 개발(Dev) 평가이며 Gate 6 Holdout이 아닙니다. `quasi_holdout`은 이름과 달리 "추가 Dev" 세트입니다. v5와의 비교는 별도 리포트(`RAG_V5_VS_FINAL_COMPARISON_REPORT_20260920.md`)에 있습니다.

## 1. 최종 결과 (350문항)

| 지표 | 값 | 근거 |
| --- | ---: | --- |
| Recall@5 | 0.792 | 정답 문항 322건 가중 |
| MRR@5 | 0.677 | 〃 |
| Citation Precision | 0.324 | 255 / 787 |
| Citation Coverage | 0.792 | Recall과 수치상 동일(이 평가 구조의 특성) |
| Abstention Precision | 0.857 | 18 / 21 |
| Abstention Recall | 0.643 | 18 / 28 |
| Faithfulness (LLM-judge) | 0.716 | 판정 276건 |
| Answer Relevancy (LLM-judge) | 0.645 | 판정 303건 |
| LLM 호출 실패율 | 0.07% | 2 / 2916 |
| 문항 실행 오류 | 0건 | 350문항 전부 완료 |

세트별:

| 지표 | dev150 | policy100 | 추가 Dev 100 |
| --- | ---: | ---: | ---: |
| Recall / MRR | 0.810 / 0.718 | 0.790 / 0.655 | 0.762 / 0.631 |
| Citation Precision | 0.405 | 0.287 | 0.268 |
| Abstention P / R | 6/8 / 6/8 = 0.750 / 0.750 | 해당 없음(정답 보류 0건, 예측 0건) | 12/13 / 12/20 = 0.923 / 0.600 |
| Faithfulness (판정 수) | 0.733 (116) | 0.719 (89) | 0.683 (71) |
| Answer Relevancy (판정 수) | 0.633 (128) | 0.776 (87) | 0.534 (88) |
| LLM 호출 실패 / 호출 | 1 / 920 | 0 / 1050 | 1 / 946 |

세 세트 모두 `quality_metrics_valid=True`이고, 모델은 Qwen/Qwen3.5-9B, `--top-k 5`입니다.

## 2. 이 결과가 만들어진 방법

1. **기준 실행 a:** 3개 세트 전체를 현재 코드로 실행 (`artifacts/evaluation/20260919/`). 350문항 전부 완료, LLM 호출 실패 27건(0.92%).
2. **실패 원인 진단:** 실패한 호출을 노드별로 추적해 **N5 claim 추출에서 길이 초과(`finish_reason=length`)로 잘린 것이 주된 원인**임을 확인했습니다. 잘린 6건 전부가 N5였고 N13은 없었습니다. 잘린 응답을 보면 N5 프롬프트에 출력 형식 지시가 없어 모델이 JS 코드나 배열 등 제멋대로의 형식으로 길게 쓰는 경우가 많았습니다.
3. **코드 변경 (N5만):** N5 호출에 `max_tokens=2048`을 지정했습니다(`LLM_MAX_NEW_TOKENS_CLAIM_EXTRACT`, 호출 시점에 읽음, 잘못된 값이면 기본값). 프롬프트는 바꾸지 않았고 N13은 올리지 않았습니다. 토큰이 늘면 파싱에 성공하는 출력이 늘어 N7이 `ValueError`로 파이프라인을 죽일 수 있는 경로를 막기 위해, 지원하지 않는 `required_aspects` 제거와 중복 `reasons` 제거를 함께 넣었습니다.
4. **재실행과 병합:** a에서 LLM 호출이 실패한 23문항(dev150 9, policy100 2, 추가 Dev 12)만 새 코드로 파이프라인과 답변 품질 판정까지 다시 돌리고, a 결과의 해당 문항만 교체했습니다. 지표는 기존 함수(`calculate_summary`, `summarize_answer_quality`)로 다시 계산했습니다(`scripts/merge_evaluation_reruns.py`). 점수 계산식은 바꾸지 않았습니다.

**a 대비 변화:** 위 방법의 효과는 LLM 호출 실패 27건 → 2건(0.92% → 0.07%)이고, 품질 지표는 변동 범위 안입니다(Recall 0.792 → 0.792, MRR 0.680 → 0.677, Citation Precision 0.319 → 0.324, Faithfulness 0.711 → 0.716, Answer Relevancy 0.655 → 0.645). 23문항 중 21문항에서 LLM 실패가 사라졌고, 남은 2문항(`dev-form-marine-145`, `pol-391000000152`)은 2048에서도 길이 초과입니다.

## 3. 시도했다가 되돌린 것: N5 출력 형식 힌트

N5 프롬프트에 출력 형식·분량 제한(종류당 claim 1개, reasons 최대 3개)을 넣는 변경도 시도했습니다.
- LLM 호출 실패는 0에 가깝게 줄었습니다(23문항 진단: 실패 7 → 0). 기존 프롬프트에서는 N5 호출 54건 중 53건이 규칙 기반으로 폴백되던 것이 새 프롬프트에서는 36~48%로 줄었습니다.
- 그러나 전체 재평가에서 Answer Relevancy(0.655 → 0.510)와 오보류(policy100 0 → 7건)가 나빠졌습니다. 이 비교는 그 사이 들어간 다른 변경(검색어 정제, 평가 근거 구성)과 섞여 있어 **원인을 N5 힌트로 단정하지 못했고**, 힌트는 롤백했습니다(`fe657e9`).
- 이 실험에서 확인된 잠재 결함 2건(N7이 죽는 경로)은 토큰 변경에 방어 코드로 남겼습니다. 한 번 발생한 문항 오류의 원인은 트레이스백으로 확인했습니다: 중복 `reasons`가 N7의 `ValueError`를 일으켰습니다.

## 4. 한계

- **일부 문항만 재실행해 반영한 결과입니다.** 전체 350문항을 새 코드로 다시 돌린 것이 아닙니다. 전체 재실행에서는 새로 실패하는 문항이 나올 수 있어 LLM 실패율 0.07%는 이 병합 방식에서의 값입니다.
- **코드 상태가 문항마다 다릅니다.** 재실행 23문항은 커밋 `5f5a98f`(검색어 정제, 평가 근거 구성 변경) 위에서, 나머지 327문항은 그 이전 코드에서 나온 결과입니다. 이 차이의 영향은 분리하지 못했습니다.
- **재실행은 답변까지 다시 생성한 것**이라 LLM 비결정성 때문에 실패와 무관한 점수도 바뀝니다. 품질 지표 차이(±0.01 안팎)는 변동 범위 안이고, 이 결과로 "품질이 좋아졌다"고 말할 수 없습니다. 개선은 "LLM 호출 실패 감소"에 한정됩니다.
- **실행 간 변동을 재지 않았습니다.** 같은 코드를 반복 실행한 적이 없습니다. 같은 코드로 policy100을 두 번 돌렸을 때 Recall이 0.747과 0.700으로 흔들린 관측이 한 번 있습니다(원인 분리 안 됨).
- **N5는 여전히 대부분 규칙 기반으로 폴백될 가능성이 큽니다.** 기존 프롬프트에서 N5 LLM 출력의 대부분(53/54)이 형식 이탈로 폴백됐고, 토큰 상향은 잘림 실패만 없앱니다. 이번에 폴백 비율을 다시 측정하지는 않았습니다.
- **Citation Precision 0.324는 구조적 상한이 있습니다.** 답변이 최대 5개 정책을 인용하는데 정답은 대개 1개라서 정책 ID 단위 지표가 낮게 나옵니다. 개선 여부보다 지표 정의가 제품 판단 사항입니다.
- **Abstention은 정답 보류가 28건뿐**이라 표본이 작습니다. 이번 Recall 상승(0.607 → 0.643)은 정답 보류 1건이 바뀐 영향입니다.
- **Faithfulness/Answer Relevancy는 판정 LLM이 답변 생성 모델과 같아 자기평가 편향이 있고,** 판정 표본이 350문항 중 276~303건입니다.
- **진짜 Holdout이 없습니다.** 위 350문항은 모두 개발 과정에서 본 세트입니다. Gate 6용 사전 동결 Holdout은 아직 없습니다.

## 5. 남은 과제

1. N5 프롬프트의 출력 형식 문제(형식 이탈, 대부분 폴백)는 그대로입니다. 프롬프트 개선은 다른 변경과 분리한 실험으로 해야 하고, 그 전에 실행 간 변동을 재야 합니다.
2. 남은 LLM 실패 2건(2048에서도 길이 초과)의 원인은 확인하지 않았습니다.
3. Faithfulness와 Answer Relevancy의 판정 편향 및 판정 표본 누락(350문항 중 약 13~21%)을 줄일 방법이 필요합니다.
4. Gate 6용 독립 Holdout을 동결해야 합니다.

## 6. 재현

```powershell
$env:PYTHONUTF8="1"
# a: 3개 세트 전체 실행 (세트별 --questions/--output-dir만 바꿈)
python scripts/run_model_evaluation.py --questions data/evaluation/dev_questions.jsonl `
  --output-dir artifacts/evaluation/20260919/dev150 --top-k 5 --workers 4 `
  --provider-concurrency 10 --judge-workers 4 --model-name test-20260919
# LLM 실패 문항만 재실행 (--question-ids에 쉼표로 문항 ID)
python scripts/run_model_evaluation.py ... --question-ids <실패 문항 ID들> --model-name test-20260920e
# 병합
python scripts/merge_evaluation_reruns.py --base <a results jsonl> --rerun <재실행 results jsonl> --name dev150
# 산출물: artifacts/evaluation/20260919/, 20260920e/, 병합 요약 20260920e/merged_summary.json
```
